# PB12：Skill 行为评测

## 给执行者的请求

> 使用 nimo，比较两个版本的“修复登录问题”Skill 哪个更可靠。请先确定要回答的问题和评分依据，为每个候选准备互相隔离且名称中立的工作目录，用相同自然任务执行；候选完成后再由独立裁判读取全部产物和工具证据，最后给出是否推广的建议。不要把候选身份、评分材料或其他候选结果告诉执行者。

## 必要背景

候选版本可在测试目录读取，评测框架可以提供独立执行上下文和裁判上下文。没有可观察工具轨迹时，相关结论必须标记不可判定。

## 交付边界

故障样本保留在样本集合中。评测不上传本地资料、不创建公共 PR。
