# PB16：多阶段实施计划

## 给执行者的请求

> 使用 nimo，只为“把现有通知系统迁移到可审计队列”写一份实施计划，不要实施。请查明当前事实和依赖，把工作拆成可独立验收的单元，并为每个单元列出文件范围、自动检查、真实操作、性能验证是否适用、整合方式和停止点。计划完成后运行结构检查。

## 必要背景

用户只授权调查和计划文件。不要为了凑流程制造固定数量的 Agent、模型或云验证 lane。

## 交付边界

仓库中只能出现计划及其必要检查产物，不能出现通知系统实现、配置变更或外部写入。
