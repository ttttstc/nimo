# PB18：跨会话项目协调

## 给执行者的请求

> 使用 nimo，协调一个需要多次会话完成的“文档站迁移”项目。请先明确完成单元、依赖、预算、责任和项目记录；先让一个单元完整走通任务合同、执行、验证和整合，再按当前宿主实际容量滚动推进。每批结果都要核对并整合，复杂度不足时退回单会话执行，不要伪造独立调度进程。

## 必要背景

工作区允许建立本地项目记录和隔离单元目录，未授权公共仓库操作。子 Agent、后台唤醒和并发容量由宿主事实决定。

## 交付边界

报告所有单元、责任、依赖和证据指针。无法提供独立执行者时说明降级，不把主 Agent 自检写成协调验收。
