# PB10：保持视觉一致

## 给执行者的请求

> 使用 nimo，把设置页恢复到提供的截图基线。请固定浏览器、视口、数据状态和字体，先确认基线与比较方式，再逐个处理组件；每次用同一操作面截图比较，默认要求像素差为零。若平台渲染确需容差，必须在修改前说明测量方法和阈值。不要修改基线图片。

## 必要背景

候选有可运行页面、固定基线截图和比较工具。用户授权页面实现和本地验证，不授权改比较工具或基线。

## 交付边界

缺少实际截图或基线时只能报告无法通过，不能以 DOM、单元测试或口头描述替代视觉验证。
