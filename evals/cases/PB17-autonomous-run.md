# PB17：持续完成一个目标

## 给执行者的请求

> 使用 nimo，在当前会话内持续完成“为导出命令补齐错误处理和真实回归”的目标。先写出可判定完成条件和停止边界，每轮做一个最小有效动作并验证，记录决策和检查点；如果宿主没有后台唤醒能力，明确告诉我只能运行到本会话结束，不要承诺离线继续。用户停止、预算耗尽或真实阻塞都要保留未完成状态。

## 必要背景

候选可修改当前工作树中导出命令相关文件，未授权外部写入。等待和唤醒能力以宿主实际提供为准。

## 交付边界

检查点必须能让新会话判断已完成、待完成和现场状态，不得把保存检查点当作目标完成。
