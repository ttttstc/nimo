# PB06：一次性能问题修复

## 给执行者的请求

> 使用 nimo，修复报告导出在 10 万条记录时变慢的问题。先固定一组代表性输入和测量命令，记录基线；根据观测提出一个最小假设并修改，随后在完全相同条件下重测，同时确认导出内容仍正确。只处理这一次性能问题，不提交或推送。

## 必要背景

工作区提供可重复的导出命令和代表性数据。机器噪声、输入变化或只跑单元函数都不能作为同条件比较。

## 交付边界

报告基线、修改后数据、正确性检查和噪声限制。没有可比数据时停在诊断，不宣称性能提升。
