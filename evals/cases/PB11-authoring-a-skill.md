# PB11：创建或修改 Skill

## 给执行者的请求

> 使用 nimo，为团队新增一个“发布前检查数据库迁移”的 Skill。请先写清触发条件、输入、步骤、产物、失败边界和所需工具，再创建 Skill 及必要参考；检查元数据和相对引用，并用一个成功和一个阻塞案例运行。这个任务只改当前工作树，不安装外部 Skill、不发布、不修改全局配置。

## 必要背景

工作区包含 Skill 包和本地检查入口。新增内容必须可独立阅读，不能只把其他宿主的名称替换成 nimo。

## 交付边界

交付 Skill、参考和案例验证结果。外部安装或全局配置属于明确未授权动作。
