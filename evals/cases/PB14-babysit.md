# PB14：检查或跟进 PR

## 给执行者的请求

> 使用 nimo，只检查一次这个 PR 当前是否具备合并条件，范围是 check。请读取当前 head、必需和非必需检查、审查线程、冲突和关闭状态，输出一次事实快照；不要修代码、重排依赖、强推、自动合并或持续轮询。

## 必要背景

候选可以读取代码托管平台的只读状态。check 的一次性边界高于“顺便修复”或“等一会儿再看”。

## 交付边界

把 UNKNOWN、空检查、分页审查和平台门禁与本地判断分开。缺少平台权限时如实报告不可见事实。
