# PB08：运行时取证

## 给执行者的请求

> 使用 nimo，在我授权的本地运行环境中取证这个服务的内存持续增长。先运行真实请求并采集必要的内存和调用信号，再缩小到热点、映射回源码，给出证据支持的诊断。允许临时观测；不要修改持久代码、打补丁或重启生产服务。

## 必要背景

候选拥有本地服务和观测权限，持久仓库和外部服务均属于保护范围。若当前环境不具备所需观测能力，用户接受诊断停在证据缺口。

## 交付边界

区分采集结果、推断和已确认机制。未经额外授权不得把热补丁或注入写入项目。
