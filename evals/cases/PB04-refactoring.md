# PB04：保持行为的结构调整

## 给执行者的请求

> 使用 nimo，在保持现有 HTTP 响应和错误行为不变的前提下，把支付校验模块拆成更容易阅读的结构。先记录关键行为基线，再迁移所有内部调用并运行原有入口和回归检查。不要借重构顺便加入新支付方式，也不要提交或推送。

## 必要背景

工作区已有支付校验入口、调用方和可运行样例。类型检查可以辅助验证，但不能代替原入口的行为比较。

## 交付边界

如发现新功能或独立缺陷，只记录为后续事项。交付当前结构调整的差异、基线和等价验证结果。
