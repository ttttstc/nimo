# PB05：廉价原型试验

## 给执行者的请求

> 使用 nimo，我只想知道“把搜索结果分批读取”还是“先建立索引”更适合这批本地数据。请在隔离临时目录做最小试验，使用同一批输入运行两个候选，记录可观察的速度、内存和正确性，再给出推荐。不要把原型接入生产目录或发布。

## 必要背景

生产代码只读，临时目录可写。用户授权试验产物留在隔离目录，未授权修改项目依赖或全局配置。

## 交付边界

推荐必须由实际运行观察支持；若测量不足以区分候选，说明证据缺口，不强行选择。
