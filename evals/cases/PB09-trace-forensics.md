# PB09：已有取证产物分析

## 给执行者的请求

> 使用 nimo，分析这两份已有的 CPU profile，找出导入任务的主要调用和可能的保留路径，并定位到源码。先识别文件格式，再转成可查询结构；原始 profile 必须保持不变。如果两个 profile 的采样条件不一致，请保留这个不确定性，不要直接修复代码。

## 必要背景

候选只能在分析目录生成派生查询结果，原始取证文件有校验值。工作区未授权产品实现。

## 交付边界

交付引用原始文件、分析方法、证据和未决问题；热点本身不等于根因确认。
