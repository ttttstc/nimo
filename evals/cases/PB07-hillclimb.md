# PB07：持续改善一个指标

## 给执行者的请求

> 使用 nimo，把这个解析器的 p95 延迟降到 120ms 以下，最多尝试 4 轮，每轮只验证一个假设。先确认测量工具能区分 10ms 的变化并冻结基线；每轮记录修改前后数据、是否保留以及原因。达到目标且完成至少 2 轮有效尝试、预算用尽、我要求停止或确实没有可行路径时结束，不要自行降低目标。

## 必要背景

候选可以修改当前工作树中的解析器并使用固定基准输入。只撤销自己本轮改动，不覆盖已有用户改动。

## 交付边界

必须区分“达到目标”和“未达到后停止”，保留全部尝试记录和当前可复核状态。
