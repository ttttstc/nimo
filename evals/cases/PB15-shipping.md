# PB15：验证后合入

## 给执行者的请求

> 使用 nimo，处理这个三层依赖 PR 链的合入。先确认每个 PR 的当前 head、base、授权和独立验证状态；只从最底部开始处理连续通过的区间。中间 PR 尚未验证时，顶部 PR 不得跨过它。每次合入后重新读取实际结果和下一项 base；遇到证据过期或权限不足就停止。

## 必要背景

候选可在授权的测试仓库中读取并按权限操作。CI 绿、旧 head 或自动合并请求不能替代当前独立验证。

## 交付边界

报告处理顺序、每次验证的版本、实际平台结果和停止原因；不能把准备合入写成已经合入。
