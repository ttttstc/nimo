# Playbook 移植账本（P0 批次 D：autopilot-full、autopilot-stack）

上游固定版本：pstack/skills/poteto-mode/playbooks @ 93b00b89ef425a9c1bac0d0b317dfc49c930ac99。本表记录 PB19 autopilot-full 与 PB20 autopilot-stack 相对 upstream 的每个有意义适配；纯语言翻译不算差异，不记录。semantic impact 说明适配后行为等价、增强还是降级；verification 说明核对方式。upstream 的 `--force-with-lease`、state-then-wait、一 PR 一 owner、patch-id 重验、副作用判定进度等强约束均原样保留语义，不入本表。

| artifact | upstream rule | nimo adaptation | reason | semantic impact | verification |
|---|---|---|---|---|---|
| autopilot-full.md 步骤 1 / autopilot-stack.md 步骤 3 | 明确开始后用 `/goal` 布防完整程序目标，目标跨轮次保持到队列／链完成 | 宿主提供原生目标机制时用它承载；否则用 state.mjs 初始化 program 并把目标写入 goal，此后每轮迭代与每个审计 tick 重读 | `/goal` 为 Cursor 私有接口 | 有能力宿主语义等价；无能力宿主以本地持久记录降级，跨轮次目标语义保留 | 人工对照 tools.md state.mjs 节与 host-contract.md 持续运行节 |
| autopilot-full.md 步骤 2 / autopilot-stack.md 步骤 1 | 每个 PR 一个 Cursor cloud agent 作为 owner 承担完整生命周期 | 每个 PR 一个宿主独立执行者（独立子 Agent／独立上下文）；宿主无独立执行者时按宿主合同降级并在交付中如实说明，owner 工作可由主 Agent 串行承担但必要的独立验证无法完成即停在未验证状态 | Cursor 私有接口（cloud agent）宿主无关化 | 有能力宿主等价；无子 Agent 宿主诚实降级且独立验证受阻时明确停止 | 人工对照 host-contract.md 降级节（其中点名 autopilot） |
| autopilot-full.md 步骤 2 / autopilot-stack.md 步骤 1 | forge 解析：`command -v origin` 成功且 Origin 能解析仓库时用 `origin pr ...` 做 PR 创建、编辑、查看、观察、合并，否则留在 `gh` 并记录回退；绝不要求 Graphite | 默认 GitHub（`gh`）；用户配置的当前 forge CLI 能解析该仓库时用它的等价命令做 PR 创建、编辑、查看、观察和合并操作，否则留在 `gh` 并记录回退；不要求任何栈管理工具 | Origin／Graphite 为 upstream 特定 forge 工具，nimo 走当前 forge＋Git 原生 | 等价（与 babysit／shipping 已移植的同一 forge 规则对齐） | 人工对照 babysit.md 步骤 1、shipping.md 步骤 1 |
| autopilot-full.md 步骤 2 | 自证引用 prove-it-works 原则 Skill；真实操作面用 cursor-team-kit 的 control-cli／control-ui 按变更所需驱动 | 自证链接 [nimo-principle-prove-it-works](../../../skills/nimo-principle-prove-it-works/SKILL.md)；真实操作面按 [nimo-verify](../../../skills/nimo-verify/SKILL.md) 选择实际工具驱动 | cursor-team-kit 为 Cursor 专属插件工具 | 等价（真实操作面验证语义不变，工具选择改为宿主相关 Skill） | 人工对照 shipping.md 步骤 1 同规则 |
| autopilot-full.md 步骤 2/4、autopilot-stack.md 步骤 1/4 | Bugbot 分诊按 `../references/bugbot-triage.md`；去噪用 cursor-team-kit 插件的 deslop（`/deslop`）；`/no-comments`（no-comments Skill）；swarm、show-me-your-work | 机器人审查意见按 [审查分类](../../../skills/nimo-mode/references/review-triage.md) 怀疑分诊；去噪用 nimo-deslop；nimo-no-comments；nimo-swarm；nimo-show-me-your-work | 命名适配（bugbot-triage→review-triage，插件 Skill→nimo Skill） | 等价 | check-package 名称白名单与相对链接校验通过 |
| autopilot-full.md 步骤 6 / autopilot-stack.md 步骤 2 | 审计 tick 由本地 root 以真实终端 `/loop` 承载（monitored-shell 30 分钟 sleep＋output-notification sentinel）；云端 root 用既有 cloud-sleeper 唤醒链；绝不把节奏交给记忆或易丢失的完成通知 | 节奏由真实唤醒机制承载（宿主原生定时器／自动化）；宿主无后台／定时能力时审计并入当前会话的清收点推进，并如实说明会话结束即停止 | `/loop` 与 cloud-sleeper 为 Cursor 私有机制 | 有能力宿主等价；无能力宿主诚实降级（仅当前会话运行），“不交给记忆／易丢失通知”的强约束保留 | 人工对照 host-contract.md 持续运行节 |
| autopilot-full.md 步骤 6 / autopilot-stack.md 步骤 2 | 每个 tick 用 `git show origin/main:pstack/.../autopilot-*.md` 从 trunk 重读本 Playbook | 每个 tick 从安装位置重读本 Playbook 的当前正文 | nimo 是安装的 Skill 包，不以特定仓库 trunk 布局为前提；等价语义是“对照当前权威版本审计，不信上下文里的旧副本” | 等价（审计基准都是当前权威版本；nimo 不保证运行期自动同步远端，若包在运行中更新，以新读到的版本为准） | 人工复核 |
| autopilot-stack.md 步骤 6 | 追加时用 `origin pr create --status open --base <parent-branch>` 或 `gh pr create --base <parent-branch>`；改 base 用 `origin pr edit`／`gh pr edit`；绝不通过 Graphite 提交或登记链 | 按已解析 forge 用等价命令：GitHub 上新建 PR 用 `gh pr create --base <父分支>`，已有 PR 改 base 用 `gh pr edit <pr> --base <父分支>`；绝不通过栈管理工具提交或登记链 | Origin CLI 不在 nimo 工具面；gh 为当前 forge 默认 | 等价 | 人工对照 tools.md PR 状态与依赖链节 |
| autopilot-full.md / autopilot-stack.md 必要条件与停止 | 无对应表述 | 增加授权边界：push、评论、合并、rebase 等外部动作受用户当前授权约束；宿主无独立执行者时必要独立验证无法完成即停在未验证状态，不伪装独立 | nimo 授权边界增强（host-contract 降级节点名 autopilot） | 相比 upstream 增加约束，无行为损失；`--force-with-lease` 前先 `git ls-remote` 核对远端版本这一 upstream 安全纪律原样保留 | 人工对照 host-contract.md 降级节 |
| autopilot-full.md 交付 / autopilot-stack.md 交付 | Reply＝队列（owner／状态／head SHA、verdict 及产出 swarm、合并与下一步、会签及理由、打开的门禁、决策轨迹位置）；链根／链尖链接、每环节一行 verdict、搁置与排除原因 | 与 nimo 既有交付契约（返回结果、当前产物版本、实际验证和证据、未完成项及跳过原因、不自动授予提交／推送／评论／合并／发布权限）合并 | 保留 nimo 结构约定 | 增强（两份契约都保留） | 人工复核交付段 |
| autopilot-full.md / autopilot-stack.md 交叉引用 | swarm／show-me-your-work／babysit／shipping／orchestrate／autopilot-full 等名称 | nimo-swarm、nimo-show-me-your-work 等命名适配；babysit／shipping／orchestrate／autopilot Playbook 名保持不变 | 命名适配 | 等价 | check-package 名称白名单与相对链接校验通过 |
