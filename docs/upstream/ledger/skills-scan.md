# Skill 移植账本：verification-create／verification-maintain／skill-author／skill-evaluate／deslop／verify

- 上游来源：pstack（cursor/plugins 仓库，固定 SHA `93b00b89ef425a9c1bac0d0b317dfc49c930ac99`）：
  - `skills/create-verification-skill/`（SKILL.md + references/feature-map-example/ 三文件）→ nimo-verification-create，其验证方法论另流入 nimo-verify；
  - `skills/maintain-verification-skill/SKILL.md` → nimo-verification-maintain；
  - `skills/poteto-mode/playbooks/authoring-a-skill.md` → nimo-skill-author 的方法论来源；
  - `skills/poteto-mode/playbooks/eval.md` → nimo-skill-evaluate 的方法论来源（编排入口为 nimo-mode/playbooks/eval.md，即 PB12，已由 Playbook 任务恢复并在其中委托本 Skill 承载案例与运行规则）；
  - `skills/unslop/SKILL.md` → nimo-deslop 的一部分语义来源（过程纪律）；upstream deslop Skill 本体在 cursor-team-kit 插件，不在本仓库检出中，pstack 内的行为描述（`docs/guide/05-build-and-clean.md`、`skills/poteto-mode/SKILL.md`、`playbooks/multi-phase-plan.md`）为 nimo-deslop 的主要语义来源；
  - nimo-verify 无一一对应的上游 Skill：方法论来自 create-verification-skill 的证据标准与 `docs/guide/06-verify-and-ship.md`。
- 本账本只记录相对上游的有意义适配。纯中文化翻译与统一命名适配不逐条记录，包括：`/create-verification-skill` 等斜杠命令 → nimo Skill 名、原则名加 `nimo-principle-` 前缀、粗体引用改为正文内联相对链接。
- 语义等价要求：上游中所有改变 Agent 决策/执行行为的强约束（MUST/NEVER 级语义）、停止条件、判定信号、anti-pattern、evidence/verdict 规则、模板与 rubric 条目、"为什么这个步骤存在"均已逐条保留等价语义，未压缩成口号。

| artifact | upstream rule | nimo adaptation | reason | semantic impact | verification |
| --- | --- | --- | --- | --- | --- |
| 6 个 SKILL.md frontmatter | create/maintain 上游含 `disable-model-invocation: true` | 未保留该字段 | Cursor 私有元数据；nimo 宿主无关，基本元数据仅依赖 name/description（host-contract.md 3.8） | 无行为语义损失；触发方式交由宿主与用户决定 | tests/assets/check-package.mjs 只校验 name/description |
| 6 个 SKILL.md | 上游无授权边界段 | 统一新增"边界与交付"段，引用 host-contract.md 与 delegation.md | nimo 授权边界增强（允许适配第 5 条） | 新增授权约束，不放松任何上游语义 | check-package.mjs 校验两个引用文件存在 |
| nimo-verification-create/SKILL.md 生成位置 | 生成 `.cursor/skills/verify-<app>/`，索引为 `features/README.md` | 生成 `.nimo/verification/`，索引为 `features/index.md` | nimo 项目验证资产的包内既定约定（nimo-verify、nimo-verification-maintain 均引用该路径） | 无；索引文件名是约定不是行为语义 | 三个 Skill 中路径引用一致 |
| nimo-verification-create/SKILL.md 第 2 节 | 生成物带 YAML frontmatter（`name: verify-<app>` + description），"without frontmatter the skill never registers" | 生成的验证 Skill 不要求 frontmatter，改为开头一段写明验证对象（应用、操作面、何时用） | Cursor 的 Skill 注册是宿主私有机制；nimo 生成的资产是项目文档，不是宿主注册的 Skill | 发现/触发语义由 nimo-verify"优先读 .nimo/verification/SKILL.md"承接；"写明何时用"的 description 语义保留 | 人工复核 |
| nimo-verification-create/references/feature-map-example/ | 三个英文示例文件（README.md、create-note.md、search.md） | 移植为中文；README.md 改名 index.md 对齐 nimo 布局；`control-notes`／`notes`／按钮名等示例专属字面量原样保留（驱动约定要求"每条命令按字面执行"） | 示例承载功能地图契约（恰好四个 H2、前置条件开头、动作+命令+可观察结果配对、证明与跳过报告规则） | H2 名称本地化为 子功能／用户视角入口／用 &lt;harness&gt; 驱动／陷阱，SKILL.md 与示例逐字一致 | 人工核对 SKILL 第 3 节与示例的 H2 契约一致；链接均指向真实文件 |
| nimo-verification-create/SKILL.md 第 3 节 | "The map is the repo's maintained verification source; a proof that drives one convenient entry point is incomplete when the map lists others" | 原样恢复（"地图是本仓库受维护的验证事实源……只驱动一个方便入口的证明是不完整的"），并在 nimo-verify 步骤 1 复现 | 该判定信号改变验证完整性标准 | 恢复 | 两处对照 |
| nimo-verification-create/SKILL.md | 上游无：与用户确认主路径、验证状态标注（已验证／未验证／受阻）、Git 提交＋工作区差异标识（不能只记 HEAD）、凭据与来源不明指令不进地图、初始化报告 | 保留为 nimo 增强（第 1、3 节与边界段） | nimo 增强结构保留 | 授权与诚实边界增强，不与上游冲突 | 边界段逐条保留 |
| nimo-verification-maintain/SKILL.md 源码波 | "One read-only subagent per feature file, launched concurrently. Children never drive the app and never edit files. Return shape: …" | 每个功能文件一个只读子 Agent，并发派发；宿主不支持并发时按委派纪律串行独立执行并记录降级；子 Agent 绝不驱动应用、绝不改文件；返回形状（功能摘要／源码入口／疑似漂移或无／一条配方）原样保留 | Cursor 并行子 Agent 私有接口 → 宿主原生能力 + 诚实降级 | 并行不再是硬性前提；隔离与返回形状语义不变 | delegation.md"降级与必要条件"条款 |
| nimo-verification-maintain/SKILL.md 实际运行 | "doctor before first drive, doctor on each fresh session …, doctor again after any failed drive"等三条不变量、"verified-unreachable 只有带具体前提和已尝试路径才可记"、harness 修复交付前必须重新实际驱动、最终拆除在最后一次驱动（含重证）之后 | 全部逐条恢复（步骤 4），含"体检看不到的失败就重置到已知状态或重启，而不是祈祷" | 当前版本丢失了停止条件与判定信号 | 恢复：失败处置、不可达判定、拆除时序均为行为语义 | 对照 upstream Pass 4 |
| nimo-verification-maintain/SKILL.md 交付 | "changed → one PR of proven corrections, re-read every changed file first; clean/blocked → no PR" | 已修正：一次交付包含全部已证修正，交付前先重读每个改过的文件，按本次授权执行 PR 或本地可审查结果；无变更／受阻：不交付 | nimo 授权边界：PR 创建受用户当前授权约束 | 授权外语义（一次交付全部已证修正、先重读改动）保留 | 边界与交付段 |
| nimo-verification-maintain/SKILL.md | 上游 triage：doc drift／harness gap／product gap（record for the user, keep it out of this PR）；harness fix 遵守与生成相同的 helpers 规则 | 映射进既有三分类表；工具缺口行并入"可执行、调用方式写在验证 Skill 正文"的辅助规则；产品缺陷行保留"不修改预期掩盖问题，产品修复作为单独任务" | 结构融合（nimo 三分类表是既有结构） | 无语义损失 | 对照 upstream Pass 5 |
| nimo-verification-maintain/SKILL.md | 上游无：日常增量维护（Bug／Feature 任务按需验证）、分类存疑请用户裁决、巡检结果写入维护信息 | 保留为 nimo 增强 | nimo 增强 | 保留 | 保留段落 |
| nimo-skill-author/SKILL.md 步骤 1 | "Use the create-skill skill (Cursor's built-in for authoring SKILL.md files)" | 本 Skill 自身内建编写与验证方法，不依赖宿主内置工具（nimo-v1-issue-13-spec 的既定映射） | nimo 宿主无关 | 无；方法内容全部内建 | 步骤 1-6 承载方法 |
| nimo-skill-author/SKILL.md | "When in doubt, delete; prose earns its keep by changing a decision. Tell it to do the thing and skip the reason. Explain only when the rule is confusing without one. Match tone to scope. Point at structural sources…; hardcoded details go stale… Delegate to other skills by path; don't restate. A workflow you keep hitting but isn't captured → propose a new skill." | 逐条恢复（步骤 1、3、4 与文末独立行） | 当前版本丢失这些写作纪律（均为行为约束） | 恢复 | 对照 authoring-a-skill.md |
| nimo-skill-author/SKILL.md 步骤 8 | "Run **Opening a PR**" | 按本次授权交付，PR 整理链接 nimo-mode 的 opening-a-pr playbook | Graphite/Cursor 流程 → forge + Git 原生 + 授权约束 | 无；"先清理再交付"入口保留 | 链接指向真实文件 |
| nimo-skill-author/SKILL.md 回复 | "Reply: summary of the skill, key design decisions, validation notes" | 交付段：Skill 概要、关键设计决定、验证注记 | 翻译 | 无 | 交付段 |
| nimo-skill-evaluate/SKILL.md 结构 | 上游单文件 eval.md | 拆为 nimo-mode/playbooks/eval.md（编排入口，PB12）+ 本 Skill（案例定义与运行规则的权威来源）；PB12 原文"案例定义与运行规则遵循 nimo-skill-evaluate" | nimo 结构：playbook 编排、Skill 承载规则 | 无；两处内容一致，盲化条款双写以保证任一入口可独立执行 | 与 PB12 逐条对照 |
| nimo-skill-evaluate/SKILL.md 步骤 6 | "read each candidate's local transcript under the active workspace's `agent-transcripts/` directory (the system prompt names this path). Do not glob across `~/.cursor/projects/*/`" | 读宿主为当前工作区记录的每个候选的执行轨迹（工具调用与文件访问记录）；不跨工作区搜索宿主的私有会话存储；宿主不提供执行轨迹时，涉及"实际读取"的评分记不可判定，不用自报顶替 | Cursor 私有 transcript 目录 → 宿主原生轨迹 + 诚实降级（与 PB12 既有适配逐字一致） | 无轨迹时从"读轨迹"降级为"记不可判定"，比上游更明确地禁止自报顶替 | 与 PB12 第 6 步措辞一致 |
| nimo-skill-evaluate/SKILL.md 步骤 4/5 | "per the **arena** skill's Phase B / Phase C"、"different models"、"a different model family" | 链接 nimo-arena 阶段 B／阶段 C；用户配置的模型，未配置时在用户可用模型上各开一个，仅单模型可用时按委派纪律降级并记录 | 固定模型 → 用户配置的模型（允许适配第 4 条） | 模型多样性改由用户配置决定，降级路径显式化 | check-package.mjs fixed-model 模式不触发 |
| nimo-skill-evaluate/SKILL.md 盲化条款 | 7 条不可协商条款（禁止词表、自然提示、不诱发链式引用、消毒命名、不告知其他候选、裁判只见消毒标签、单裁判单尺度一次运行评两组）+ 观察者效应 | 全部逐条恢复（"盲化不可协商条款"节） | 当前版本丢失禁止词表与消毒规则（均为行为约束） | 恢复 | 对照 eval.md Non-negotiables |
| nimo-skill-evaluate/SKILL.md | 上游无：触发与归属确认、第一版案例覆盖清单、预算与停止条件、发布与回归退回规则、跨模型分组报告、秘密与不可信内容 | 保留为 nimo 增强 | nimo 增强结构保留 | 保留 | 对应段落 |
| nimo-deslop/SKILL.md 来源 | upstream deslop Skill 本体在 cursor-team-kit 插件（pstack README"not shipped here"），不在本仓库 | 依据 pstack 内行为描述重建：四类 slop 目标（叙述性注释、无依据守卫、死兼容路径、无关改动，见 docs/guide/05-build-and-clean.md）+ 提交前时序（poteto-mode SKILL"Before commit → the deslop skill"、multi-phase-plan"Run /deslop before each commit"） | 无法逐句对照本体；pstack 内描述是可获得的最权威来源 | 四类目标与时序语义来自 pstack 描述，非臆造 | 人工复核上游引文 |
| nimo-deslop/SKILL.md 前导段 | "cleanup is not optional polish. A diff with narrating comments and defensive dead weight reads as unfinished to reviewers, and the extra code is where the next bug hides. If the diff feels padded, say deslop it before you commit, not after review calls it out." | 逐句恢复为前导段 | 当前版本丢失时序规则与理由 | 恢复：提交前清理的时机约束 | 前导段 |
| nimo-deslop/SKILL.md 步骤 | unslop 过程：扫描模式 → 重写（保留含义、匹配语气）→ 自查"what makes this obviously AI generated?" | 移植为代码版：扫描模式（步骤 1-2）→ 重写（步骤 3）→ 自查"这个差异里还有哪些地方一眼就是顺手生成的凑数？"（步骤 5） | unslop 是散文 Skill；仅过程纪律适用于代码（任务指定的"一部分语义来源"） | 过程语义等价，模式清单换成代码语境 | 步骤 1/3/5 |
| nimo-deslop/SKILL.md 分工 | "The division of labor is worth keeping straight. /deslop cleans slop out of the code, /unslop cleans it out of prose, /no-comments hands the comments to a reviewer who didn't write them." | 恢复为分工段（nimo-deslop／nimo-unslop／nimo-no-comments），并注明本 Skill 是作者侧自查、不替代独立评审 | 当前版本丢失三 Skill 边界 | 恢复：防止重复清理或互相推诿 | 分工段 |
| nimo-deslop/SKILL.md | 上游 deslop 本体不可考；当前版本的 nimo 独有内容（优先标准库和既有机制、不删用户要求能力／边界校验／错误处理、不顺手格式化无关代码、只清理本任务制造的问题） | 保留为 nimo 增强 | nimo 增强结构保留 | 保留 | 步骤 3/4 与边界段 |
| nimo-verify/SKILL.md 来源 | 无一一对应上游 Skill；方法论来自 create-verification-skill 证据标准 + docs/guide/06-verify-and-ship.md | 恢复："能编译"不是证据（guide 06 开篇）、按变更类型选检查（CLI／UI／解析器迁移／性能／存储五对，guide 06）、真实用户路径不走内部 setter 或测试专用端点、捕获动作与结果状态、副作用与可见结果一同验证、mock 只在产品边界已隔离处、dry-run 通过观察验证实际跳过什么、体检（进程／版本／端口归属／鉴权）、稳定句柄优先、绝不按进程名杀、证据在清理后于指名位置确认、"没有证据的自信结论是危险信号" | 当前版本丢失上述全部判定与证据标准 | 恢复 | 对照 create-verification-skill 第 2 节与 guide 06 |
| nimo-verify/SKILL.md 步骤 2 | 上游方法论假定可用浏览器／CDP、PTY 等驱动手段 | 限定为"当前实际存在的终端、浏览器、设备或 API 工具"，缺失不伪造（nimo-v1-issue-13-spec："不发明 GUI 控制 API"） | nimo 宿主无关约束 | 与上游"existing harnesses first"精神一致；缺失时受阻而非硬凑 | 步骤 2 |
| nimo-verify/SKILL.md | 上游无：更换 Shell 时确认参数含义相同、参数转发失败不误判为业务结果、独立验证由未参与实现的上下文执行 | 保留为 nimo 增强 | nimo 增强 | 保留 | 步骤 5/7 |
