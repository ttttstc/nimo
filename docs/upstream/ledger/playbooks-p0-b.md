# P0 Playbook（PB14–PB16）移植账本

上游固定版本：pstack/skills/poteto-mode/playbooks @ 93b00b89ef425a9c1bac0d0b317dfc49c930ac99（babysit.md、shipping.md、multi-phase-plan.md）。本表记录三个 Playbook 相对 upstream 的每个有意义适配；纯语言翻译与统一命名映射（pstack Skill 名 → nimo 前缀、bugbot → 机器人审查意见、review threads → 审查意见串、merge frontier → 合入前沿）不逐条记录。semantic impact 说明适配后行为等价、增强还是降级；verification 说明核对方式。

| artifact | upstream rule | nimo adaptation | reason | semantic impact | verification |
|---|---|---|---|---|---|
| babysit.md 首段 | 声明本 Playbook 替换 Cursor 内建 babysit skill，描述匹配也不路由过去 | 删除该声明；Mode 路由表（SKILL.md）已固定 babysit 为唯一入口 | nimo 无内建同名竞争 Skill，宿主也无该路由机制 | 无：不存在被抢占的路由目标 | 人工核对 nimo-mode/SKILL.md 路由表 |
| babysit.md 步骤 1 forge 解析 | `gh` 默认；`command -v origin` 成功且 Origin 能解析仓库时改用 `origin pr ...`；永不要求 Graphite | 默认 GitHub（`gh`）；用户配置的当前 forge CLI 能解析该仓库时用等价命令，否则留在 `gh` 并记录回退；不要求任何栈管理工具 | Origin 为 Cursor 私有 forge；nimo 首个 forge 集成是 GitHub（tools.md §3.9），其他 forge 由宿主现有工具提供等价事实 | 等价：forge 只解析一次、记录回退、不引入栈工具依赖的语义保留 | 人工复核；全文无 Origin 专属命令名 |
| babysit.md 步骤 6 状态来源 | `scripts/watch-pr/watch-pr`：默认 JSON、`--pretty`、check 模式 `--status-only`、裸命令轮询到终态、四列表格，输出 READY/WAITING/ADVANCE/COMPLETE | `inspect-pr.mjs` 单次 JSON 事实查询（含 mergeability、必需检查、未解决审查项、数据缺失）；`check` 模式一次查询即止；轮询到终态是 drive 行为，由宿主等待机制重复调用完成；ADVANCE 语义移植为"前沿被他人合入→以新前沿继续" | nimo 本地工具契约：只读事实、有限请求、不无限轮询（tools.md §3.9） | 等价：事件唤醒交宿主持有；verdict 判定语义（READY／非终态 WAITING（merge-queue）／他人合入推进／队列完成）逐条保留 | 人工对照步骤 6 与 tools.md 的 PR 事实结论定义 |
| babysit.md 步骤 6 持续运行 | `drive`/`background` 在 `/loop` dynamic mode 下运行；watcher 为事件唤醒加长回退心跳 | 在宿主持续运行机制下运行（事件唤醒 + 长回退心跳，见宿主合同 §持续运行）；宿主无后台唤醒时明确仅当前会话运行，会话结束即停止并保存检查点 | `/loop` 为 Cursor 私有接口；宿主合同规定诚实降级 | 无后台唤醒能力的宿主上为诚实降级，其余等价 | 人工核对步骤 6 与 host-contract.md |
| babysit.md 步骤 3 双跟进者 | 两个 babysitter 互踩；"a cloud one plus a local one produce it twice" | 一个在宿主的其他会话或执行者、一个在当前会话，同样发生两次；当前会话无法核实其他会话时先向用户确认 | Cursor 云端 Agent 概念宿主无关化；跨会话可见性无保证 | 等价 + 增强：无法核实时显式向用户确认，不假设独占 | 人工复核 |
| babysit.md 步骤 6 Origin 停止条件 | Origin 停在 merge-ready：检查绿、`origin pr view` 报可合并无阻塞、`origin pr thread list` 无未解决阻塞；不等 GitHub watcher verdict | 用户配置的其他 forge 以其自身可合并状态为准：检查全绿、PR 视图报可合并且无阻塞、意见串无未解决阻塞项；不等 GitHub 特有观察 verdict | Origin 命令不可用；判定标准泛化到当前 forge 的等价事实 | 等价泛化 | 人工复核步骤 6 第三段 |
| babysit.md 步骤 8 回复命令 | Origin 用 `origin pr thread reply --body-file`；GitHub 用 `gh api --method POST .../replies --input <payload.json>`，正文放 JSON 文件 | 保留"回复正文通过参数或文件输入传递、绝不拼进 shell"与 GitHub 命令示例；其他 forge 用其等价命令；发布受评论授权约束 | 命令面泛化；安全规则不变 | 等价 + 授权约束增强（评论受用户当前授权约束） | 人工核对；check-package 无 shell 拼接违规 |
| babysit.md 步骤 8 轮数来源 | GitHub 用 watcher 的 Bugbot pass count；Origin 从 `origin pr thread list` 与审查历史推导 | 从审查事实与历史推导；GitHub 上用 `inspect-pr.mjs` 的审查事实 | nimo 无 watcher 输出 | 等价 | 人工复核 |
| babysit.md 步骤 6/9 合并授权 | watcher re-arm 不授权合并；仅用户明确要求合并/落地/发布/就绪即合才执行，路由到 shipping | 同语义保留；另按 nimo 授权边界声明 push、评论等外部动作受用户当前授权约束，交付段不自动授权 | nimo 移植规则要求增加授权边界 | 增强：在 upstream 限制之上显式声明授权边界 | 人工核对必要条件与交付段 |
| babysit.md 步骤 6 冻结清单 | 队列栈 PR 清单自底向上捕获一次，同一冻结清单传给每次 rearm | 同语义；明确可记入 program.json 的前沿字段（state.mjs） | nimo 本地状态工具已有该字段 | 增强：清单有持久记账位置，判定语义不变 | 人工核对 tools.md frontier 字段定义 |
| shipping.md 步骤 1 验证执行者 | 每 PR 一个子 Agent，各为 Cursor 云 Agent，各自用 control-ui/control-cli（cursor-team-kit）驱动真实操作面，对照父版本与 head | 每 PR 一个独立执行者（宿主独立子 Agent 或独立上下文），不批量；多执行者按 nimo-swarm 组织；真实操作面按 nimo-verify 选择实际工具，对照父版本与 head | Cursor 私有子 Agent 接口与套件名宿主无关化 | 等价：独立性、逐 PR、真实操作面、对照 base/head 语义保留 | 人工核对步骤 1；宿主合同降级规则 |
| shipping.md 步骤 1 verdict 记录 | verdict 发布到它自己的 PR 上，让记录活得比会话长 | 评论授权允许时发布到对应 PR；无评论授权时在交付中给出完整 verdict 记录并说明缺口 | nimo 授权边界：评论为外部动作 | 等价 + 授权约束：无授权时不丢失记录，改为会话内交付并说明 | 人工核对步骤 1 与交付段 |
| shipping.md 步骤 1 无独立执行者 | 隐含云 Agent 总可用 | 按宿主合同降级：shipping 必要的独立验证无法完成时停在未验证状态，不用源码审阅或 CI 绿冒充 | 宿主合同 §降级与必要条件已有该规则 | 诚实降级：单会话宿主上明确受阻而非伪造 | 人工核对必要条件与 host-contract.md |
| shipping.md 步骤 4/5 命令 | `origin pr edit/merge`、`gh pr edit/merge --squash/--auto`；Origin `--auto` 与 GitHub `--auto` 语义区分 | 保留 `gh` 命令与 squash/auto 语义；其他 forge 用等价命令，开关语义以当前 forge 为准；rebase、推送、改 base、合并均在用户对本流程的授权范围内执行，未授权停止报告 | 命令面泛化 + nimo 授权边界 | 等价 + 授权增强 | 人工核对步骤 4/5 |
| shipping.md 步骤 8 观察 | `watch-pr --queued-stack` 仅作事件唤醒；每次唤醒后轮询 `gh pr view --json state,mergedAt,mergeStateStatus,statusCheckRollup,autoMergeRequest`；`/loop` 持有观察 | 观察只作事件唤醒；唤醒后查同一组字段（`gh pr view --json` 或 `inspect-pr.mjs` 事实）；观察在宿主持续运行机制下持有，无后台唤醒时仅当前会话并保存检查点 | nimo 无 watcher 脚本；持续运行交宿主 | 等价：三类硬失败条件、BLOCKED 非失败、忽略 READY 直到 MERGED、不用 babysit 停止条件全部保留；无后台宿主为诚实降级 | 人工对照步骤 8 与 upstream 原文 |
| shipping.md 步骤 6 autoMergeRequest | 不证明 Origin merge-when-ready 已布防等四项 | "Origin merge-when-ready"改为"其他 forge 的 merge-when-ready"，四项否定清单保留 | Origin 不可用 | 等价泛化 | 人工核对步骤 6 |
| multi-phase-plan.md 步骤 3 探索 | 用 `subagent_type: "poteto-agent"` 子 Agent 探索并显式指定模型 | 用宿主独立子 Agent（独立上下文）探索；模型按用户配置 | Cursor 私有子 Agent 类型与固定模型名禁用 | 等价：独立上下文、返回文件指针而非转储的语义保留 | check-package 固定模型名模式通过；人工核对 |
| multi-phase-plan.md 验证段 | "Ten lanes on grok-4.6-fast-xhigh"：固定十条 lane、固定模型 | lane 数量与模型按变更所需和用户配置决定，不固定条数、不固定模型；unit/live/perf 三类验证、live 强制、trunk 回归 lane、双边 perf 门、trunk 缺功能时的绝对预算规则全部保留 | 移植指令明确允许移除固定 lane 数与固定模型，验证含义须保留 | 等价：移除的是数量与模型，验证语义不变 | 人工对照"验证规则"节与 upstream Verification 段 |
| multi-phase-plan.md Control skill 段 | control-ui（浏览器/Electron/Web）、control-cli（CLI/TUI）、原生移动用仓库模拟器 Skill | 按操作面选择驱动方式：浏览器自动化、终端、仓库现有模拟器驱动能力；无驱动手段的操作面入附录 C 且如实写验证受阻 | cursor-team-kit 为 Cursor 私有套件 | 等价泛化：按面选工具、双面双 lane、缺口入风险附录的语义保留 | 人工核对"操作面驱动"节 |
| multi-phase-plan.md 骨架 Arm the program | `/goal` 固化执行目标（计划路径、PR 顺序、验证规则、谁合并、完成条件）；30 分钟审计 tick，本地真终端循环、云 sleeper 唤醒链；tick 提示词逐字 | 执行目标写入 program.json 并在每次启动和恢复时传递（state.mjs 已有）；审计节奏由用户配置或计划指定，不靠记忆维持；无后台唤醒时仅当前会话运行；tick 的审计内容（重读执行 Playbook 与目标、修漂移、只按副作用判进展、叫停卡 lane、发状态表）以"审计内容"条目保留 | `/goal` 与固定 tick 为 Cursor 私有接口；宿主合同要求节奏不凭记忆、无后台能力诚实降级 | 等价 + 诚实降级：目标固化与审计内容语义保留；固定 30 分钟改为按配置 | 人工核对"计划骨架"布防与审计内容条目 |
| multi-phase-plan.md 骨架 Boot recipe | 每条 live lane 在自己的云 VM 上跑 PR head，经 control-ui/control-cli 驱动，截图存 /tmp/swarm-<pr-id>/worker-<n>/ | 每条 live lane 在 PR head 的独立隔离环境运行：检出确切 head SHA、启动后端和操作面等就绪、输入只经操作面驱动方式、截图存带 PR 和 lane 标识的路径并随报告返回 | 云 VM 为 Cursor 私有资源；隔离环境由宿主 worktree/目录提供 | 等价：隔离、head 固定、只经操作面输入、证据路径语义保留 | 人工核对"计划骨架"真实 lane 启动配方 |
| multi-phase-plan.md 骨架整体 | 以代码块内嵌完整 markdown 骨架（How to read this、Program checklist、Arm/Spawn owners、PR mechanics、Verdict and merge、Boot recipe、每 PR 节、Close、附录 A–D） | 单元结构由 [计划模板](../../../skills/nimo-mode/references/plan-template.md) 承载（目标/文件/依赖/可观察结果/自动检查/真实操作/性能验证/停止点）；程序级节与附录以"计划骨架"必备节清单移植，顺序保持，每节内容逐项保留 | nimo 已有独立计划模板文件与确定性检查器，骨架不重复内嵌 | 结构等价：生成计划必含同样的节与规则；排版由模板承载 | 人工对照"计划骨架"与 upstream 骨架代码块 |
| multi-phase-plan.md 步骤 6 检查器 | `node pstack/skills/poteto-mode/scripts/check-plan.mjs` 强制骨架形状、每验证块的验证规则、标点规则 | `node skills/nimo-mode/scripts/check-plan.mjs <plan.md>` 强制单元标题与八个字段、依赖引用与环、未解决占位符；"修掉每一行输出"语义保留 | nimo 检查器按 nimo 计划结构实现（encode-lessons-in-structure） | 等价目标（确定性计划检查），检查规则随 nimo 计划结构调整；upstream 标点规则由写作 Skill 承载 | 对照 scripts/check-plan.mjs 实现 |
| multi-phase-plan.md 步骤 5 写作命令 | `/technical-writing` 全文写作后 `/unslop`；两条逐字规则与标点规则 | nimo-technical-writing 全文写作后 nimo-unslop；"不要任何抽象比喻""像海明威一样写作"两条规则与不用长破折号、句中不用冒号保留 | 斜杠命令为 Cursor 私有接口 | 等价 | 人工核对步骤 5 |
| multi-phase-plan.md 步骤 4 计划路径 | 操作者未指定时写到 agent store 的 docs/ | 写到任务工作区的 docs/ 目录 | agent store 为 Cursor 私有存储 | 等价 | 人工核对步骤 4 |
| multi-phase-plan.md 骨架 Arm（trunk 重读清单） | 启动时 `git show origin/main:` 逐个读执行 Playbook、swarm、control skill、opening-a-pr 及其他叶 Skill，每次 tick 重读 | 启动时从 trunk 读执行 Playbook、nimo-swarm、操作面驱动说明、opening-a-pr 和其他所用 Skill 的当前版本，每次唤醒重读 | pstack 路径换 nimo 结构；control skill 泛化为操作面驱动说明 | 等价：防旧副本语义保留 | 人工核对"计划骨架"布防条目 |
| multi-phase-plan.md 所需 Skill | upstream 正文不引用 architect；引用 how、interrogate、show-me-your-work、swarm、technical-writing、unslop | 列表按移植后正文实际引用重排：how、swarm、verify、deslop、no-comments、technical-writing、unslop、interrogate、show-me-your-work；移除摘要版误列的 nimo-architect | 移植指令：所需 Skill 按正文实际引用调整 | 无行为损失：设计取舍走 Mode 层通用规则（nimo-architect 由 nimo-mode/SKILL.md 第 5 条覆盖） | 人工复核（需确认移除 architect 可接受） |
