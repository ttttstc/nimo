# sandbox 变更记录

## v0.2.0

- 新增 `--top N` 选项：不区分大小写统计词频，输出频率最高的 N 个单词（频率降序、同频按字母升序）；N 超出单词总数时输出全部单词。
- 新增 `--version` 选项：输出版本号。
- 不带 `--top` 时行为与 v0.1.0 完全一致。

## v0.1.0

- 初始版本：`wordcount.py` 统计文本单词数。
